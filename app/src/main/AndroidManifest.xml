<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.xxhoz.secbox">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
            android:name="com.xxhoz.secbox.App"
            android:allowBackup="false"
            android:icon="@mipmap/logo"
            android:label="@string/app_name"
            android:largeHeap="false"
            android:networkSecurityConfig="${networkSecurityConfig}"
            android:roundIcon="@mipmap/logo"
            android:supportsRtl="true"
            android:theme="@style/Theme.XArch"
            tools:replace="android:allowBackup,android:networkSecurityConfig,android:supportsRtl"
            tools:targetApi="n">
        <activity
                android:name="com.xxhoz.secbox.module.start.StartActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
                android:name="com.xxhoz.secbox.module.main.MainActivity"
                android:screenOrientation="portrait"
                android:theme="@style/ActivityTheme">
        </activity>
        <activity
                android:name="com.xxhoz.secbox.module.about.AboutActivity"
                android:screenOrientation="portrait"
                android:theme="@style/ActivityTheme" />
        <activity
            android:name="com.xxhoz.secbox.module.player.AGVideoActivity"
            android:screenOrientation="portrait"
            android:theme="@style/ActivityTheme" />
    </application>

</manifest>
